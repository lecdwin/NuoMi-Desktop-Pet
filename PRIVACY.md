# 隐私说明

糯米桌面宠物是一款离线运行的 Windows 桌面程序。

## 程序会读取什么

开启“跟着键盘鼠标动（Bongo）”且宠物可见时，程序会接收：

- 虚拟键的按下和松开状态；
- 鼠标位置；
- 鼠标左右键、中键和滚轮状态。

这些状态只用于决定画面上哪一个键帽亮起、哪只爪子落下，以及头部看向哪里。

## 程序不会做什么

- 不把按键组合、还原或保存成输入文字；
- 不记录键盘历史；
- 不记录鼠标点击历史；
- 不读取剪贴板；
- 不截图或录屏；
- 不需要账号；
- 不包含广告或统计 SDK；
- 不向网络上传任何数据。

关闭 Bongo 模式、隐藏宠物或退出程序后，全局键鼠监听会停止。

## 本机保存的数据

程序会在当前 Windows 用户的注册表中保存：

- 宠物位置和大小；
- 置顶、跟随鼠标、Bongo 和自主互动等选项；
- 饥饿、活力、心情、亲密度和无聊度；
- 首次提示是否已经显示。

位置：

```text
HKEY_CURRENT_USER\Software\NuoMiDesktopPet
```

如果用户主动开启“开机自动启动”，程序还会在当前用户启动项中保存 EXE 路径：

```text
HKEY_CURRENT_USER\Software\Microsoft\Windows\CurrentVersion\Run
```

程序不需要管理员权限。

## 清除本机数据

先退出糯米并取消开机自启，然后可以在注册表编辑器中删除：

```text
HKEY_CURRENT_USER\Software\NuoMiDesktopPet
```

不熟悉注册表的用户无需手动清理；这些数据只占用很少空间。
